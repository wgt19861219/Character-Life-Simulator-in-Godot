# 阶段 1：时间系统 + 活动持续行为 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐步实施。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 给角色加游戏时钟 + 活动持续锁定 + 时段过滤,使其呈现昼夜节律(夜间睡、白天活动),玩家可调速/暂停。

**Architecture:** TimeManager Autoload 单例管时间;Character_Class 加 `tick(delta, day_part)` 自治(decay + effects 按 per_hour 独立叠加,`actual` 只钳 effects);活动表升级为 `{effects:per_hour毛值, duration_hours, allowed_during}`;Main 撤 Timer 改 `_process` 读单例。

**Tech Stack:** Godot 4.x(实测 4.7 stable)/ GDScript / 无第三方测试框架(用 godot MCP 的 `execute_gdscript` 断言 + `run_project` 集成)

**Spec:** `D:\GitHub\Character-Life-Simulator-in-Godot\docs\superpowers\specs\2026-06-22-stage1-time-design.md`

## Global Constraints

- 项目路径:`D:\GitHub\Character-Life-Simulator-in-Godot`
- Godot 二进制:`D:\godot\Godot_v4.6.2-stable_win64.exe`(import 用;run_project 用 4.7,均已验证兼容)
- GDScript 用 **tab 缩进**(项目惯例,不可用空格)
- 不引入 GUT 测试框架(YAGNI);测试用 `mcp__godot__script` 的 `execute_gdscript` action(load_autoloads=true)+ `mcp__godot__runtime` 的 `run_project`
- 中节奏:1 现实秒 = 5 游戏分;速度档 1x/2x/3x + 暂停(独立 bool)
- effects 存 per_hour **毛值**;decay 也 per_hour;两者在 tick 内独立叠加(sleeping 时 food 照常 decay)
- `actual = min(hours, remaining_hours)` 只钳 effects;decay 用完整 hours
- 活动结束**只解锁**(`is_busy=false`),不调 apply —— `apply_activity` / `update_need` / `decay_needs` 删除
- 每个任务的 commit 命令默认给出,但**是否真 commit 由用户在执行 handoff 时确认**(项目全局规则:不主动 commit)

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `TimeManager.gd` | 游戏时钟、时段查询、速度/暂停、consume_delta | 新建 |
| `project.godot` | 注册 TimeManager 为 Autoload | 改(加 `[autoload]` 段) |
| `Character_Class.gd` | 状态、tick 自治、活动表、效用、选择 | 重写 |
| `Main.gd` | 驱动 tick、GUI、键盘输入、config decay 重标定 | 重写 |
| `node_2d.tscn` | 删 Timer + 加 Time/Status/Speed 三 Label | 改 |

---

### Task 1: TimeManager 单例 + 时段查询

**Files:**
- Create: `D:\GitHub\Character-Life-Simulator-in-Godot\TimeManager.gd`
- Modify: `D:\GitHub\Character-Life-Simulator-in-Godot\project.godot`(加 `[autoload]`)
- Test: `execute_gdscript`(load_autoloads=true)

**Interfaces:**
- Produces: Autoload 单例 `TimeManager`,方法 `get_hour()->int`、`get_day_part()->String`、`get_clock_string()->String`、`consume_delta()->float`、`set_speed(n)`、`toggle_pause()`;属性 `game_minutes:float`(初值 480)、`speed_scale:float`、`paused:bool`

- [ ] **Step 1: 写 TimeManager.gd**

Create `TimeManager.gd`(tab 缩进):

```gdscript
extends Node

const BASE_MIN_PER_SEC: float = 5.0

var game_minutes: float = 480.0
var speed_scale: float = 1.0
var paused: bool = false
var _last_consumed_game_minutes: float = 480.0

func _process(delta: float) -> void:
	if not paused:
		game_minutes += BASE_MIN_PER_SEC * speed_scale * delta

func get_hour() -> int:
	return int(floor(game_minutes / 60.0)) % 24

func get_day_part() -> String:
	var h := get_hour()
	if h >= 22 or h < 6:
		return "night"
	if h < 12:
		return "morning"
	if h < 18:
		return "afternoon"
	return "evening"

func get_clock_string() -> String:
	var total := int(floor(game_minutes)) % 1440
	var hh := total / 60
	var mm := total % 60
	return "%02d:%02d" % [hh, mm]

func consume_delta() -> float:
	var d := game_minutes - _last_consumed_game_minutes
	_last_consumed_game_minutes = game_minutes
	return d

func set_speed(n: float) -> void:
	speed_scale = n

func toggle_pause() -> void:
	paused = not paused
```

- [ ] **Step 2: project.godot 注册 Autoload**

在 `project.godot` 的 `[application]` 段**之前**插入(位置任意,但通常 autoload 在前):

```
[autoload]

TimeManager="*res://TimeManager.gd"
```

- [ ] **Step 3: 写失败测试(execute_gdscript 断言时段边界 + consume_delta 初值)**

用 `mcp__godot__script`(action=execute_gdscript, load_autoloads=true)跑:

```gdscript
var tm = TimeManager
var cases = {0:"night",5:"night",6:"morning",11:"morning",12:"afternoon",17:"afternoon",18:"evening",21:"evening",22:"night",23:"night"}
var fail = []
for h in cases:
	tm.game_minutes = float(h * 60)
	if tm.get_day_part() != cases[h]:
		fail.append("h=%d got=%s want=%s" % [h, tm.get_day_part(), cases[h]])
tm.game_minutes = 480.0
tm._last_consumed_game_minutes = 480.0
if tm.consume_delta() != 0.0:
	fail.append("consume_delta init != 0")
if tm.get_hour() != 8:
	fail.append("get_hour at 480min != 8, got %d" % tm.get_hour())
print("PASS" if fail.is_empty() else "FAIL " + str(fail))
```

- [ ] **Step 4: 跑测试,确认 PASS**

Expected: 输出 `PASS`

- [ ] **Step 5: Commit(待用户确认)**

```bash
cd "D:/GitHub/Character-Life-Simulator-in-Godot" && git add TimeManager.gd project.godot && git commit -m "feat(stage1): add TimeManager autoload singleton"
```

---

### Task 2: Character_Class 全面升级(活动表 + tick + 效用 + 选择)

**Files:**
- Modify(重写): `D:\GitHub\Character-Life-Simulator-in-Godot\Character_Class.gd`
- Test: `execute_gdscript`

**Interfaces:**
- Consumes: Task 1 的 `TimeManager`(本任务单元测试不依赖,集成时 Main 传入 day_part)
- Produces: `Character_Class` 新契约 —— `tick(delta_minutes:float, day_part:String)`、`select_best_activity(day_part:String)`(改签名,选中即锁定)、`get_activities(day_part,_name)->Dictionary`、`calculate_utility(activity:Dictionary)->float`(读 `effects` 子表)、状态字段 `current_activity`/`remaining_hours`/`is_busy`;删除 `apply_activity`/`update_need`/`decay_needs`

- [ ] **Step 1: 重写 Character_Class.gd**

Write 覆盖 `Character_Class.gd`(tab 缩进,完整内容):

```gdscript
class_name Character_Class
extends Node

var Name_of_character

var sleep_max
var food_max
var entertainment_max
var social_max
var health_max
var physical_max
var mental_max

var sleep_decay
var food_decay
var entertainment_decay
var social_decay
var health_decay
var physical_decay
var mental_decay

var sleep
var food
var entertainment
var social
var health
var physical
var mental

# 活动持续状态
var current_activity: String = ""
var remaining_hours: float = 0.0
var is_busy: bool = false

const DEFAULT_DURATION_HOURS: float = 2.0

func _init(config = {}):
	Name_of_character = config["character_name"]
	sleep_max = config["sleep_initial_max"]
	food_max = config["food_initial_max"]
	entertainment_max = config["entertainment_initial_max"]
	social_max = config["social_initial_max"]
	health_max = config["health_initial_max"]
	physical_max = config["physical_initial_max"]
	mental_max = config["mental_initial_max"]
	sleep_decay = config["sleep_initial_decay"]
	food_decay = config["food_initial_decay"]
	entertainment_decay = config["entertainment_initial_decay"]
	social_decay = config["social_initial_decay"]
	health_decay = config["health_initial_decay"]
	physical_decay = config["physical_initial_decay"]
	mental_decay = config["mental_initial_decay"]
	sleep = round(sleep_max / 2)
	food = round(food_max / 2)
	entertainment = round(entertainment_max / 2)
	social = round(social_max / 2)
	health = round(health_max / 2)
	physical = round(physical_max / 2)
	mental = round(mental_max / 2)

# 全部活动表。effects 为 per_hour 毛速率;duration_hours 未标吃默认 2h;allowed_during 未标则任意时段。
var list_of_activities = {
	"sleeping": {"effects": {"sleep": 15, "health": 2, "mental": 3}, "duration_hours": 8, "allowed_during": ["night"]},
	"take_a_nap": {"effects": {"sleep": 12}, "duration_hours": 1, "allowed_during": ["afternoon", "evening"]},
	"eating_at_home": {"effects": {"food": 55, "health": 4}, "duration_hours": 1},
	"eating_out": {"effects": {"food": 30, "social": 5, "entertainment": 5}, "duration_hours": 2},
	"grocery_shopping": {"effects": {"food": 15, "physical": 5}, "duration_hours": 2},
	"going_to_the_gym": {"effects": {"physical": 12, "health": 5}, "duration_hours": 2},
	"socializing_at_cafe": {"effects": {"social": 8, "food": 10}, "duration_hours": 2},
	"watching_movie": {"effects": {"entertainment": 10, "mental": -1}, "duration_hours": 2},
	"reading": {"effects": {"mental": 12, "entertainment": 5}, "duration_hours": 1},
	"working_overtime": {"effects": {"mental": -3, "physical": -4, "food": -5}, "duration_hours": 4, "allowed_during": ["morning", "afternoon"]},
	"going_to_doctor": {"effects": {"health": 12}, "duration_hours": 2},
	"playing_sports": {"effects": {"physical": 12, "social": 5, "health": 4}, "duration_hours": 2},
	"taking_a_bath": {"effects": {"health": 8, "mental": 6}, "duration_hours": 1},
	"cooking": {"effects": {"food": 20, "mental": 4}, "duration_hours": 1},
	"going_to_a_concert": {"effects": {"entertainment": 8, "social": 5}, "duration_hours": 3},
	"visiting_family": {"effects": {"social": 8, "mental": 4}, "duration_hours": 3},
	"doing_yoga": {"effects": {"health": 8, "mental": 8}, "duration_hours": 1},
	"online_shopping": {"effects": {"entertainment": 8, "mental": -1}, "duration_hours": 1},
	"playing_video_games": {"effects": {"entertainment": 10, "mental": -2}, "duration_hours": 2},
	"going_to_a_museum": {"effects": {"entertainment": 5, "mental": 6}, "duration_hours": 3},
	"gardening": {"effects": {"mental": 5, "physical": 5}, "duration_hours": 2},
	"taking_a_walk": {"effects": {"health": 5, "mental": 5, "physical": 5}, "duration_hours": 1},
	"going_to_the_beach": {"effects": {"entertainment": 5, "health": 4}, "duration_hours": 3},
	"visiting_a_spa": {"effects": {"health": 8, "mental": 6}, "duration_hours": 3},
	"going_fishing": {"effects": {"entertainment": 5, "mental": 4}, "duration_hours": 3},
	"painting": {"effects": {"mental": 6, "entertainment": 5}, "duration_hours": 2},
	"writing": {"effects": {"mental": 4}, "duration_hours": 2},
	"going_to_a_party": {"effects": {"social": 10, "entertainment": 10}, "duration_hours": 3},
	"volunteering": {"effects": {"social": 5, "mental": 5}, "duration_hours": 3},
	"going_to_a_library": {"effects": {"mental": 8}, "duration_hours": 2},
	"cleaning_the_house": {"effects": {"health": 4, "mental": -2}, "duration_hours": 2}
}

func _decay_need(need_name: String, hours: float) -> void:
	var rate: float = get(str(need_name) + "_decay")
	set(need_name, max(0.0, float(get(need_name)) - rate * hours))

func _apply_effects_hourly(effects: Dictionary, hours: float) -> void:
	for need in effects:
		var mx: float = get(str(need) + "_max")
		set(need, clamp(float(get(need)) + effects[need] * hours, 0.0, mx))

# 角色自治主循环。① decay 用完整 hours;② effects 用 actual=min(hours,remaining) 钳到剩余;③ 空闲则选活动。
func tick(delta_minutes: float, day_part: String) -> void:
	var hours: float = delta_minutes / 60.0
	if hours <= 0.0:
		return
	_decay_need("sleep", hours)
	_decay_need("food", hours)
	_decay_need("entertainment", hours)
	_decay_need("social", hours)
	_decay_need("health", hours)
	_decay_need("physical", hours)
	_decay_need("mental", hours)
	if is_busy:
		var actual: float = min(hours, remaining_hours)
		_apply_effects_hourly(list_of_activities[current_activity]["effects"], actual)
		remaining_hours -= hours
		if remaining_hours <= 0.0:
			is_busy = false
			current_activity = ""
	else:
		select_best_activity(day_part)

func get_activities(day_part: String, _character_name: String) -> Dictionary:
	var result: Dictionary = {}
	for act_name in list_of_activities:
		var act = list_of_activities[act_name]
		var allowed = act.get("allowed_during", [])
		if allowed.is_empty() or day_part in allowed:
			result[act_name] = act
	return result

func calculate_utility(activity: Dictionary) -> float:
	var total_utility: float = 0.0
	var duration: float = activity.get("duration_hours", DEFAULT_DURATION_HOURS)
	for need in activity["effects"].keys():
		var impact: float = activity["effects"][need] * duration
		var current_value: float = get(need)
		var max_value: float = get(str(need) + "_max")
		var base_utility: float = impact * float(max_value - current_value) / float(max_value)
		var wasted_utility: float = max(0.0, current_value + impact - max_value)
		total_utility += base_utility - wasted_utility
	return total_utility

func select_best_activity(day_part: String) -> void:
	var activities := get_activities(day_part, Name_of_character)
	var best_activity := ""
	var highest_utility := -1e9
	for activity_name in activities.keys():
		var utility := calculate_utility(activities[activity_name])
		if utility > highest_utility:
			best_activity = activity_name
			highest_utility = utility
	if best_activity != "":
		current_activity = best_activity
		remaining_hours = list_of_activities[best_activity].get("duration_hours", DEFAULT_DURATION_HOURS)
		is_busy = true
```

- [ ] **Step 2: 写失败测试(时段过滤 + busy 叠加断言)**

`execute_gdscript`(load_autoloads=true):

```gdscript
var C = preload("res://Character_Class.gd")
var jane = C.new({"character_name":"Jane","sleep_initial_max":100,"food_initial_max":100,"entertainment_initial_max":100,"social_initial_max":100,"health_initial_max":100,"physical_initial_max":100,"mental_initial_max":100,"sleep_initial_decay":6,"food_initial_decay":8,"entertainment_initial_decay":4,"social_initial_decay":3,"health_initial_decay":1,"physical_initial_decay":3,"mental_initial_decay":2})
var fail = []
# 时段过滤:night 含 sleeping、不含 working_overtime
var night = jane.get_activities("night", "Jane")
if not night.has("sleeping"): fail.append("night 缺 sleeping")
if night.has("working_overtime"): fail.append("night 不该含 working_overtime")
var morn = jane.get_activities("morning", "Jane")
if morn.has("sleeping"): fail.append("morning 不该含 sleeping")
if not morn.has("working_overtime"): fail.append("morning 缺 working_overtime 字段项")
# busy 叠加:remaining=0.5h,推 0.6h → effects 按 actual=0.5,decay 按 0.6,结束解锁
jane.sleep = 50.0
jane.is_busy = true
jane.current_activity = "sleeping"
jane.remaining_hours = 0.5
var before = jane.sleep
jane.tick(36.0, "night")  # 36 分钟 = 0.6h
# decay: 50 - 6*0.6 = 46.4; effects: +15*0.5=7.5 → 53.9
if abs(jane.sleep - 53.9) > 0.1: fail.append("sleep 叠加错: got %s want ~53.9" % str(jane.sleep))
if jane.is_busy: fail.append("结束后应 is_busy=false")
if jane.remaining_hours > 0.0: fail.append("remaining 应 <=0")
print("PASS" if fail.is_empty() else "FAIL " + str(fail))
```

- [ ] **Step 3: 跑测试,确认 PASS**

Expected: `PASS`(sleep ≈ 53.9,验证 actual 钳 effects + decay 完整 hours + 结束解锁)

- [ ] **Step 4: Commit(待用户确认)**

```bash
cd "D:/GitHub/Character-Life-Simulator-in-Godot" && git add Character_Class.gd && git commit -m "feat(stage1): rewrite Character_Class with hourly tick + activity lock"
```

---

### Task 3: Main 改造 + 场景(撤 Timer、_process 驱动、GUI、键盘、decay 重标定)

**Files:**
- Modify(重写): `D:\GitHub\Character-Life-Simulator-in-Godot\Main.gd`
- Modify: `D:\GitHub\Character-Life-Simulator-in-Godot\node_2d.tscn`(删 Timer + connection,加 3 Label)
- Test: `run_project`(启动无错 + GUI 三行显示)

**Interfaces:**
- Consumes: Task 1 `TimeManager.consume_delta()/get_day_part()/get_clock_string()`;Task 2 `Character_Class.tick/select_best_activity`
- Produces: 可运行的游戏(`run_project` 启动,角色自主过时间)

- [ ] **Step 1: 重写 Main.gd**

Write 覆盖 `Main.gd`(tab 缩进)。**注意 decay 值已重标定为 per_hour**(sleep 6/food 8/ent 4/social 3/health 1/physical 3/mental 2):

```gdscript
extends Control

var jane
var jane_config_dict = {
	"character_name": "Jane",
	"sleep_initial_max": 100,
	"food_initial_max": 100,
	"entertainment_initial_max": 100,
	"social_initial_max": 100,
	"health_initial_max": 100,
	"physical_initial_max": 100,
	"mental_initial_max": 100,
	"sleep_initial_decay": 6,
	"food_initial_decay": 8,
	"entertainment_initial_decay": 4,
	"social_initial_decay": 3,
	"health_initial_decay": 1,
	"physical_initial_decay": 3,
	"mental_initial_decay": 2
}

var sleep_label
var food_label
var entertainment_label
var social_label
var health_label
var physical_label
var mental_label
var activity_label
var time_label
var status_label
var speed_label

func _ready():
	sleep_label = get_node("SleepLabel")
	food_label = get_node("FoodLabel")
	entertainment_label = get_node("EntertainmentLabel")
	social_label = get_node("SocialLabel")
	health_label = get_node("HealthLabel")
	physical_label = get_node("PhysicalLabel")
	mental_label = get_node("MentalLabel")
	activity_label = get_node("ActivityLabel")
	time_label = get_node("TimeLabel")
	status_label = get_node("StatusLabel")
	speed_label = get_node("SpeedLabel")
	jane = Character_Class.new(jane_config_dict)

func _process(_delta):
	var dm = TimeManager.consume_delta()
	jane.tick(dm, TimeManager.get_day_part())
	update_gui()

func update_gui():
	sleep_label.text = "Sleep: " + str(int(jane.sleep))
	food_label.text = "Food: " + str(int(jane.food))
	entertainment_label.text = "Entertainment: " + str(int(jane.entertainment))
	social_label.text = "Social: " + str(int(jane.social))
	health_label.text = "Health: " + str(int(jane.health))
	physical_label.text = "Physical: " + str(int(jane.physical))
	mental_label.text = "Mental: " + str(int(jane.mental))
	activity_label.text = "Activity: " + (jane.current_activity if jane.current_activity != "" else "—")
	time_label.text = "Time: " + TimeManager.get_clock_string() + " (" + TimeManager.get_day_part() + ")"
	var status = "Idle" if not jane.is_busy else (jane.current_activity + " · 剩 " + str("%.1f" % jane.remaining_hours) + "h")
	status_label.text = "Status: " + status
	var sp = "paused" if TimeManager.paused else (str(TimeManager.speed_scale) + "x")
	speed_label.text = "Speed: " + sp + "   [1/2/3 切速 · Space 暂停]"

func _unhandled_input(event):
	if event is InputEventKey and event.pressed and not event.echo:
		match event.keycode:
			KEY_1: TimeManager.set_speed(1.0)
			KEY_2: TimeManager.set_speed(2.0)
			KEY_3: TimeManager.set_speed(3.0)
			KEY_SPACE: TimeManager.toggle_pause()
```

- [ ] **Step 2: 改 node_2d.tscn —— 删 Timer + 加 3 Label**

用 `mcp__godot__scene`:
- `remove_node`(node_path 删 `Timer` 节点);场景里的 `[connection signal="timeout" ...]` 行随节点删除一并清除(若残留,Edit 删该行)
- `add_node` 加三个 Label 子节点到根 `control`:`TimeLabel`、`StatusLabel`、`SpeedLabel`(type=Label),垂直排在现有 Label 下方(offset_top 从 ~230 起递增 30)

或直接 Edit `node_2d.tscn`:删 `[node name="Timer" ...]` 块 + 末尾 `[connection ...]` 行;追加:

```
[node name="TimeLabel" type="Label" parent="."]
layout_mode = 0
offset_left = 4.0
offset_top = 230.0
offset_right = 300.0
offset_bottom = 256.0
text = "Time:"

[node name="StatusLabel" type="Label" parent="."]
layout_mode = 0
offset_left = 4.0
offset_top = 258.0
offset_right = 300.0
offset_bottom = 284.0
text = "Status:"

[node name="SpeedLabel" type="Label" parent="."]
layout_mode = 0
offset_left = 4.0
offset_top = 286.0
offset_right = 400.0
offset_bottom = 312.0
text = "Speed:"
```

- [ ] **Step 3: 重新 import(刷新 .godot 缓存,注册 Autoload + 新脚本)**

```bash
"D:/godot/Godot_v4.6.2-stable_win64.exe" --headless --import --path "D:/GitHub/Character-Life-Simulator-in-Godot"
```

- [ ] **Step 4: 启动验证(run_project)**

`mcp__godot__runtime`(action=run_project, timeout=10)→ `get_debug_output`。

Expected:
- `errors: []`(无 Character_Class / icon / TimeManager 错误)
- `prints` 干净(无刷屏)
- 启动即跑(角色自主选活动、时间推进)

- [ ] **Step 5: Commit(待用户确认)**

```bash
cd "D:/GitHub/Character-Life-Simulator-in-Godot" && git add Main.gd node_2d.tscn && git commit -m "feat(stage1): rewire Main to TimeManager + add time/status/speed GUI"
```

---

### Task 4: 集成验证(完成标准断言)

**Files:**
- Test only(不改代码,除非验证暴露 bug)

**Interfaces:**
- Consumes: Task 1-3 完整可运行项目

- [ ] **Step 1: 长跑采样(execute_gdscript 模拟 3 游戏天,断言 night sleeping 占比 + food 不为 0)**

`execute_gdscript`(load_autoloads=true)。脚本构造 jane、手动推进时间、采样 night 段 sleeping 占比:

```gdscript
var C = preload("res://Character_Class.gd")
var jane = C.new({"character_name":"Jane","sleep_initial_max":100,"food_initial_max":100,"entertainment_initial_max":100,"social_initial_max":100,"health_initial_max":100,"physical_initial_max":100,"mental_initial_max":100,"sleep_initial_decay":6,"food_initial_decay":8,"entertainment_initial_decay":4,"social_initial_decay":3,"health_initial_decay":1,"physical_initial_decay":3,"mental_initial_decay":2})
var tm = TimeManager
tm.game_minutes = 480.0
tm._last_consumed_game_minutes = 480.0
tm.speed_scale = 1.0
tm.paused = false
# 模拟 3 游戏天,每游戏小时采一次
var night_samples = 0
var night_sleeping = 0
var food_zero_run = 0
var food_max_zero_run = 0
var steps = 24 * 3
for i in steps:
	var dp = tm.get_day_part()
	jane.tick(60.0, dp)  # 推 1 游戏小时
	tm.game_minutes += 60.0
	if dp == "night":
		night_samples += 1
		if jane.is_busy and jane.current_activity == "sleeping":
			night_sleeping += 1
	if jane.food <= 0.0:
		food_zero_run += 1
		food_max_zero_run = max(food_max_zero_run, food_zero_run)
	else:
		food_zero_run = 0
var fail = []
var ratio = (night_sleeping as float) / night_samples if night_samples > 0 else 0.0
if ratio < 0.8: fail.append("night sleeping 占比 %.2f < 0.8" % ratio)
if food_max_zero_run >= 6: fail.append("food 连续 %d 游戏小时为 0 (>=6)" % food_max_zero_run)
print("PASS" if fail.is_empty() else "FAIL " + str(fail))
```

- [ ] **Step 2: 跑断言,确认 PASS**

Expected: `PASS`(night sleeping 占比 ≥ 0.8;food 不连续 ≥ 6h 为 0)

若 FAIL:优先检查 decay/effects 标定(spec §6)与 sleeping allowed_during;若是 utility 偏差导致睡太晚(spec §9 已知限制),放宽阈值或调 sleeping per_hour。

- [ ] **Step 3: run_project 人工观察(可选,真实窗口)**

`run_project`(timeout=30),观察 GUI:时间推进、夜间 Activity=sleeping、白天切换活动、键盘 1/2/3/Space 响应。

- [ ] **Step 4: 更新路线图 + 写 Obsidian 开发日志**

- 在 `docs/superpowers/specs/2026-06-22-stage1-time-design.md` 顶部 status 改 `implemented`
- 按 CLAUDE.md 规则写 Obsidian 开发日志(`D:\workspace\Obsidian\{项目目录}\开发日志\2026-06-22 阶段1时间系统.md`),callouts 分区

- [ ] **Step 5: Commit(待用户确认)**

```bash
cd "D:/GitHub/Character-Life-Simulator-in-Godot" && git add -A && git commit -m "test(stage1): integration verify 3-day rhythm + finalize stage 1"
```

---

## Self-Review 已完成

- **Spec 覆盖**:spec §3.1-3.4 → Task 1-3;§4 数据流三条铁律 → Task 2 tick 实现 + 测试;§5 时段 → Task 2 get_activities + Task 1 get_day_part;§6 标定 → Task 2 活动表 + Task 3 decay 重标定;§7 测试 → 各 Task execute_gdscript + Task 4;§8 完成标准 → Task 4 断言。无遗漏。
- **Placeholder**:无 TBD/TODO,所有代码完整。
- **类型一致**:`tick(delta_minutes:float, day_part:String)` / `consume_delta()->float` / `get_day_part()->String` / `select_best_activity(day_part:String)` 跨任务签名一致;`current_activity`/`remaining_hours`/`is_busy` 命名统一。
- **已知风险**:Task 2 后到 Task 3 完成前,项目处于"Character 改完但 Main 仍用旧 Timer"的中间态,run_project 会报错(Character 改了签名)——这是 TDD 增量的预期中间态,Task 3 完成即恢复可运行。单元测试(execute_gdscript)在中间态仍可独立跑通。
